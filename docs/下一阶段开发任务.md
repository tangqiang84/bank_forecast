# 下一阶段开发任务

## 当前已完成

- Java 8 + Spring Boot 后端基础工程、Flyway 数据库迁移和租户鉴权。
- Vue 3 + TypeScript 前端登录、驾驶舱、银行账户和银行流水工作台。
- 银行流水 CSV 导入及导入任务记录。
- 合同、项目、合同应收计划数据库及合同 CSV 导入。
- 流水与应收 MVP 匹配，以及未知收款、部分收款、应收未收异常生成。
- P0 匹配结果确认与异常闭环（2026-09-09 验收通过）。
- P1 财务对账和可追溯性（2026-09-09 验收通过）。
- P2 分析与预测服务接入（2026-09-09 开发完成，待后续模型增强）。

## MVP 完成度盘点（2026-09-09）

按当前已确认的 MVP 功能范围，P0 匹配闭环、P1 导入质量与财务追溯、P2 预测服务接入以及预测增强均已完成。核心匹配能力当前还差 **2 项任务**：

1. 一笔银行流水拆分匹配多个应收节点，并支持按分配明细计账。
2. 多笔银行流水合并匹配一个应收节点，并与拆分匹配使用统一计账模型。

以下内容暂不计入 MVP 未完成任务数：

- “安全与投产准备”整组任务，按当前要求暂缓。
- 安全与投产准备整组任务，按当前要求暂缓。
- 模型训练、自动择优和更复杂的预测评估，属于后续增强。

因此，当前 MVP 核心功能还剩 **2 项**；预测增强已完成，但拆分/合并匹配完成前，当前版本仍不能覆盖复杂收款分配场景。

## 下一步优先级

### P0：匹配结果确认与异常闭环（已完成）

- 已增加匹配结果列表、候选依据和人工确认、拒绝接口。
- 已增加异常事项分派、备注、处理完成、关闭和操作日志接口。
- `suggested` 结果确认后才更新流水匹配状态和应收已收金额，拒绝不产生财务计账。
- 已增加重复运行幂等控制，避免同一流水重复生成匹配结果和重复计账。

验收结果：通过。用户可以查看候选依据并确认或拒绝匹配；异常按 `new -> in_progress -> resolved -> closed` 流转并记录操作日志；重复运行不会重复生成结果或重复计账。

已交付接口：

- `GET /api/v1/matching/results`
- `POST /api/v1/matching/results/{id}/confirm`
- `POST /api/v1/matching/results/{id}/reject`
- `GET /api/v1/matching/exceptions`
- `POST /api/v1/matching/exceptions/{id}/assign`
- `POST /api/v1/matching/exceptions/{id}/comment`
- `POST /api/v1/matching/exceptions/{id}/resolve`
- `POST /api/v1/matching/exceptions/{id}/close`
- `GET /api/v1/matching/exceptions/{id}/logs`

### P1：匹配准确性和导入质量

- [ ] 支持一笔流水对应多个应收节点的拆分匹配（需要新增匹配分配明细模型）。
- [ ] 支持多笔流水合并匹配一个应收节点（需要与拆分匹配统一计账模型）。
- [x] 客户名称归一化和可配置最短匹配长度，减少公司后缀、空格、标点导致的误匹配。
- [x] 合同 CSV 逐行预校验、错误行查询/CSV 下载、重复节点跳过和导入摘要。
- [x] 银行流水导入文件大小、行数、日期范围、金额小数位和金额上限校验。

验收标准：边界输入返回结构化错误，错误行可定位，重复导入不会产生重复业务数据。

本轮交付：导入任务返回 `success_rows`、`failed_rows`、`skipped_rows` 和 `error_details`；错误明细接口为 `GET /api/v1/imports/{jobId}/errors`，CSV 下载接口为 `GET /api/v1/imports/{jobId}/errors/download`。拆分/合并匹配未在本轮实现，不得用于生产计账。

### P1：财务对账和可追溯性

- [x] 增加合同应收、银行流水、匹配结果、异常事项的关联详情页。
- [x] 增加财务确认记录、确认人、确认时间和审计日志查询。
- [x] 增加按租户、合同、项目、日期、状态的筛选和分页。
- [x] 驾驶舱补齐应收总额、已收金额、逾期金额、异常数量等真实指标。

验收标准：每条匹配和异常都能追溯到原始流水、合同节点和操作人。

验收结果：通过。流水详情可查看账户、匹配结果和审计日志；合同详情可查看应收节点、关联流水和审计日志；匹配结果详情可查看候选依据、原始流水、异常和操作记录；异常详情可查看来源对象和处理日志。列表接口统一支持分页，查询按当前租户隔离；驾驶舱指标改为从应收、流水和异常表实时汇总。

已交付接口：

- `GET /api/v1/bank-transactions`：按账户、合同编号、项目编号、交易日期、匹配状态分页查询流水。
- `GET /api/v1/bank-transactions/{id}`：查询流水及其匹配、审计记录。
- `GET /api/v1/contracts`：按合同编号、项目编号、状态分页查询合同。
- `GET /api/v1/contracts/receivables`：按合同、项目、到期日期、状态分页查询应收计划。
- `GET /api/v1/contracts/{id}`：查询合同、应收节点、关联流水和审计记录。
- `GET /api/v1/matching/results`：按合同、项目、交易日期、匹配状态分页查询匹配结果。
- `GET /api/v1/matching/results/{id}`：查询匹配结果、原始流水、关联异常和审计记录。
- `GET /api/v1/matching/exceptions`：按合同、项目、日期、异常状态分页查询异常事项。
- `GET /api/v1/matching/exceptions/{id}`：查询异常来源和处理日志。
- `GET /api/v1/audit-logs`：按对象类型、对象 ID、操作类型分页查询审计日志。

分页接口统一返回 `items`、`page`、`page_size`、`total`，单页最大 100 条。

### P2：安全与投产准备

- 删除开发测试账号和本地凭据记录，禁止默认开发密码进入生产环境。
- 增加生产数据库配置、JWT 密钥轮换、密码重置和管理员初始化流程。
- 增加登录失败限流、审计日志脱敏、文件上传权限和敏感字段加密检查。
- 完善 CI：静态检查、依赖安全扫描、后端打包和前端构建。

验收标准：生产配置不含开发凭据，密钥从环境变量或密钥管理服务读取，安全扫描无高危问题。

### P2：分析与预测服务接入

- [x] 明确 analytics 服务与 Java 后端的接口契约。
- [x] 将当前租户历史流水按日汇总为净现金流，并汇总未来未收应收计划。
- [x] 增加现金流预测任务和结果落库、最新结果查询、详情查询和失败重试机制。
- [x] 前端增加预测结果、预计余额、风险等级和风险提示展示。

交付内容：

- 新增 `forecast_job`、`forecast_result` 表及 Flyway 迁移 `V4__forecast_schema.sql`。
- Java 后端通过 `ANALYTICS_BASE_URL` 调用 `POST /forecast/cashflow`，支持连接/读取超时配置。
- 预测任务状态包括 `pending`、`running`、`success`、`failed`；失败任务最多执行 3 次。
- 预测结果包含预测净现金流、未来应收、预计余额、风险等级和风险提示；查询按租户隔离。
- 前端“现金预测”工作区支持生成预测和重试失败任务。

验收结果：通过。后端编译和测试通过（22 项）；前端类型检查、单元测试（3 项）和构建通过；analytics 测试通过（2 项）。analytics 不可用时任务会落为 `failed` 并返回结构化错误，未将失败伪装为成功。

### P2：预测增强

- [x] 按预测日期回填已发生日期的真实银行净现金流到 `actual_amount`，未来日期保持空值。
- [x] 计算 `deviation_amount`、MAE、RMSE 和平均偏差。
- [x] 增加模型版本表、任务版本绑定、版本查询和启用接口。
- [x] analytics 返回并保留模型版本，当前版本为 `v1`。

验收结果：通过。预测详情支持实际金额回填和评估指标查询；模型版本可查询和启用，历史任务保留原绑定版本。

当前限制：模型版本管理已完成 MVP 能力，当前只有 `v1` 算法实现；模型训练、离线评估集、自动择优和多版本并行部署属于后续增强。

## 推荐开发顺序

1. 安全与投产准备（按当前要求暂缓）。
2. 模型训练、自动择优、离线评估集和多版本并行部署。

## 投产前清理清单

- 删除 `docs/开发测试账号.local.md`。
- 删除或禁用 `finance01` 开发账号。
- 更换 JWT 密钥、数据库凭据和所有本地测试数据。
- 重新执行完整测试、构建、安全扫描和生产配置检查。
